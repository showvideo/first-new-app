<?php
  $sql = 'SELECT title FROM anime_title WHERE *;'
  $stmt = getDB()->prepare($sql)
  $stmt->execute();
  
  $result = $stmt->fetchAll();
  echo $result;
  
  $anime_titles = array_slice($result,0,100);
  foreach($anime_titles as $anime_title) {
  echo $anime_title['title'];
  }
?>
